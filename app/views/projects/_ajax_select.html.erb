<% if @tags.size > 0 %>
  <%= select_tag "tag", options_from_collection_for_select(@tags, :id, :name), {class: "select"} %>
<% else %>
  <%= select_tag "tag", options_for_select(["---"]), {class: "select"} %>
<% end %>